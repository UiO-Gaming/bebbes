import React from "react";
import styles from "./header.module.css";

const Header = () => (
  <header className={styles.container}>
    <h1>bebbes</h1>
    <p>en brus- og pizzakalkulator</p>
  </header>
);

export default Header;
